#Selects the system with the lowest variance in DLI values

takes array of stored DLI values for final systems

calculates the standard deviation of DLI over the 12 runs through the day

picks the system with the lowest standard deviation